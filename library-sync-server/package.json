{
  "name": "library-sync-server",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted sync server for the iKame Library (assets/library-extension). SQLite catalog + on-disk file mirror; the Cocos vfx-browser extension pushes/pulls against it.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "express": "^4.21.2"
  }
}
